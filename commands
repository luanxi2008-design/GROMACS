This is the step that actually defines the simulation box in the output structure:

gmx editconf -f DMSO_2000.pdb -o DMSO_2000_6p2nm.gro -box 6.2 6.2 6.2


That .gro will have box vectors, and GROMACS viewers will show the box.

slurm file from chatgpt
#!/bin/bash
#SBATCH --ntasks=1
#SBATCH --partition=sixhour
#SBATCH --mem-per-cpu=4G
#SBATCH --time=06:00:00

#SBATCH --mail-type=ALL
#SBATCH --mail-user=xi.luan@ku.edu

#SBATCH --output=slurm-%j.out
#SBATCH --error=slurm-%j.err

#SBATCH --cpus-per-task=24

# Bind OpenMP threads to cores
export OMP_NUM_THREADS=12
export OMP_PLACES=cores
export OMP_PROC_BIND=close

module load compiler/gcc/11.4
module load gromacs/2018.8

echo "start simulation $(date)"

srun gmx mdrun -deffnm npt \
  -ntmpi 2 -ntomp 12 -npme 1 \
  -pin on -dlb auto -v

echo "finished simulation $(date)"
